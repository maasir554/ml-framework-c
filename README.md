# ML Frameword in C

## Development Note

1. Implements Arena Allocator: A better alternative for dynamic memory allocation as compared to `malloc` / `free` for this usecase.

2. Implements a Psuedo Random Generator (PRNG) to be used in ML calculatons.
